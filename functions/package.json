{
  "name": "csa-practice-functions",
  "description": "Server-side PIN/password verification for the AP CSA practice app -- moves secret checks off the client so raw PINs and password hashes are never sent to the browser, and adds attempt-based lockout that Firestore rules alone can't do.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}
